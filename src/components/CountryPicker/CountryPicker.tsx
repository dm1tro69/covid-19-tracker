import React from 'react';
import styles from './CountryPicker.module.css'

const CountryPicker = () => {
    return (
        <div>
            CountryPicker
        </div>
    );
};

export default CountryPicker;
